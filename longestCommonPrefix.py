# 编写一个函数来查找字符串数组中的最长公共前缀。
#
# 如果不存在公共前缀，返回空字符串 ""。
######################################################################
def longestCommonPrefix(ss):

    return ''
######################################################################
string = ["dog","racecar","car"]
result = longestCommonPrefix(string)
print(result)